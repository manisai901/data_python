To load the **RTO BigQuery table from the Semantic layer**, you want to move or transform data from one dataset/table to another in BigQuery. Based on your message, the source and destination are:

* **Source Table**: `corp_sec_sem.<source_table_name>`
* **Target Table**: `rto.<target_table_name>`

Here’s how you can approach this:

---

### SQL Query to Load Data from Semantic Layer to RTO Dataset:

```sql
CREATE OR REPLACE TABLE `rto.target_table` AS
SELECT *
FROM `corp_sec_sem.source_table`;
```

>  Replace `source_table` and `target_table` with your actual table names.

---

###  If You Want to Apply Transformation While Loading:

```sql
CREATE OR REPLACE TABLE `rto.target_table` AS
SELECT 
  column1,
  column2,
  UPPER(column3) AS column3_transformed
FROM `corp_sec_sem.source_table`
WHERE status = 'active';
```

---

### Using dbt (Optional, if working in Semantic Layer via dbt):

In `models/rto/target_table.sql`:

```sql
SELECT *
FROM {{ ref('source_table') }}
```

Then, in `dbt_project.yml` or your `.sql` file, you can define the target location:

```yml
models:
  rto:
    +schema: rto
```

---



* Are you loading this manually via SQL, or using a tool like dbt / Dataform / Airflow?
* Do you need to **schedule or automate** this loading process?
* Do you want to **load only incremental data** (new or updated rows)?
